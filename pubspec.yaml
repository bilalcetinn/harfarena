name: turkish_word_engine
description: A rule-based Turkish word-board analysis engine prototype.
version: 0.5.0
publish_to: none

environment:
  sdk: '>=3.3.0 <4.0.0'

dev_dependencies:
  test: ^1.25.0
